version: 1
frontend:
  phases:
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  customHeaders:
    - pattern: '**/sitemap.xml'
      headers:
        - key: 'Content-Type'
          value: 'application/xml'
